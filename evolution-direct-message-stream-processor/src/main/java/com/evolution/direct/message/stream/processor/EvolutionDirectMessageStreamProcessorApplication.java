package com.evolution.direct.message.stream.processor;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class EvolutionDirectMessageStreamProcessorApplication {

	public static void main(String[] args) {
		SpringApplication.run(EvolutionDirectMessageStreamProcessorApplication.class, args);
	}
}
