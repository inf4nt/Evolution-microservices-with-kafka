spring:
  application:
    name: 'msp-evolution-direct-message'
  cloud:
    stream:
      bindings:
        input:
          destination: 'MessageDenormalizationStateFeed'
  kafka:
    bootstrap-servers: 'localhost:9092'
    consumer:
      auto-offset-reset: earliest
  data:
    rest:
      base-path: '/state'
server:
  port: 8381